<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lens.controlmvp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.LoginActivity"/>
        <activity android:name=".ui.main.MainActivity"/>
        <activity android:name=".ui.personinfo.PersonInfoActivity"/>
        <activity android:name=".ui.changedata.ChangeDataActivity"/>
        <activity android:name=".ui.groupdetails.GroupDetailsActivity"/>
        <activity android:name=".ui.groupmember.GroupMemberActivity"/>
        <activity android:name=".ui.friend.FriendListActivity"/>
    </application>

</manifest>